apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nessie-ingress
  namespace: finlake
spec:
  ingressClassName: nginx

  rules:
  - host: nessie.4.147.239.228.nip.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: finlake-nessie
            port:
              number: 19120