name: finlake
version: 1.0.0
config-version: 2

profile: finlake

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

clean-targets:
  - target
  - dbt_packages

models:
  finlake:
    staging:
      +materialized: ephemeral
    silver:
      +schema: silver
      +materialized: table

